<div class="panel small-container">
  
  <h1 class="text-center"><%= edit ? "Edit" : "Create an" %> Category</h1>
  
  <form id="categoryEditor" action="/admin/saveCategory<%= edit ? '?id=' + category.id : ''%>" method="post">

    <div class="form-group">
      <label>Title</label>
      <input type="text" class="form-control" placeholder="Title" name="title" value="<% if (edit) { %><%= category.title %><% } %>">
    </div>

    <div class="form-group">
      <label>Brief</label>
      <textarea class="form-control" rows="5" placeholder="Instructions" name="instructions"><% if (edit) { %><%= category.instructions %><% } %></textarea>
    </div>

    <div class="form-group">
      <label>Description</label>
      <textarea class="form-control" rows="5" placeholder="Expected" name="expected"><% if (edit) { %><%= category.description %><% } %></textarea>
    </div>

    <div class="form-group">
      <input class="btn btn-primary btn-lg btn-block" type="submit"/>
    </div>
  </form>
</div>
